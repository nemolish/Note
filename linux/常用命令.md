### command
```bash
 zip -r folder.zip folder -x "*/\.*" -x "\.*"

scp [sourcefile] [target]

redis-cli -h 127.0.0.1 -p 6379

redis-server

nginx -s reload

nginx -t -c nginx.conf


tree #without argument

ln -s source_file [target_file] target_file is shortcutz

git archive -o hot-fix-20151001.zip HEAD $(git diff 5ad0f14...d75e302 --name-only)  源码

git diff 86c584a...a2e433d --name-only

zip -r richinfo_`date "+%m%d%H%M"`_v1.0.3.zip misspersion -x "*.DS_Store" -x "*.svn*"
zip -r update`date "+%m%d%H%M"`.zip cfjsg -x "*.DS_Store"  -x "*.svn*"

lsof -i tcp:1099
```